# Port d'écoute (par défaut: 8000)
# PORT=8000

# Root path FastAPI quand l'app est servie derrière un reverse proxy (ex: /neembacoaching)
# ROOT_PATH=

# Version applicative (sinon: court SHA git, sinon timestamp)
# APP_VERSION=
